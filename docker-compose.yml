version: '3.8'

services:
  backend:
    image: ghcr.io/${GITHUB_REPOSITORY_LOWER}:latest
    container_name: 180workspace-backend
    ports:
      - "4001:4001"
    env_file:
      - .env
    restart: always

  worker:
    image: ghcr.io/${GITHUB_REPOSITORY_LOWER}:latest
    container_name: 180workspace-worker
    command: ["tsx", "server.js"]
    environment:
      - RUN_MODE=worker
      - PORT=4003
    env_file:
      - .env
    restart: always
